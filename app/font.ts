import {Inter, Roboto_Mono, Lusitana} from "next/font/google";

export const inter = Inter({subsets: ['latin']})

export const RobotoMono = Roboto_Mono({subsets: ['latin'], display: 'swap', weight: '400'})

export const lusitana = Lusitana({subsets: ['latin'], display: 'swap', weight: '400'})